import { ShopOrderListsOwner } from './shop-order-lists-owner';

describe('ShopOrderListsOwner', () => {
  it('should create an instance', () => {
    expect(new ShopOrderListsOwner()).toBeTruthy();
  });
});
