import { ShopOrderListItemsOwner } from './shop-order-list-items-owner';

describe('ShopOrderListItemsOwner', () => {
  it('should create an instance', () => {
    expect(new ShopOrderListItemsOwner()).toBeTruthy();
  });
});
